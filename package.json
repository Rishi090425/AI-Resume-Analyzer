{
  "name": "ai-resume-analyzer",
  "version": "1.0.0",
  "description": "AI Resume Analyzer - Full Stack App",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "vercel-build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "multer": "^2.1.1",
    "pdf-parse": "^2.4.5"
  }
}
